allowBuilds:
  esbuild: true

# Security overrides for exact pins in @playform/compress and satori.
# Remove each once upstream bumps past the patched version.
overrides:
  deepmerge-ts: ">=8.0.0"
  svgo: ">=4.1.0"
  sharp: ">=0.35.4"
  fflate: ">=0.7.5"
